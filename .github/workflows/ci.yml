name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  disabled:
    name: CI Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Skip CI
        run: echo "CI workflow is currently disabled"
