# Supabase (required for registry DB + /submit auth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Local migrations / CLI only (do not commit; Supabase Dashboard → Database → connection string)
DATABASE_URL=

# Waitlist API (optional if /api/waitlist is enabled)
WAITLIST_FORM_SECRET=

# Registry API bundled pack clone source (optional)
# ORKESTRATE_BUNDLED_REPO_URL=
# NEXT_PUBLIC_ORKESTRATE_REPO_URL=

# GitHub OAuth is configured in Supabase Dashboard → Authentication → Providers → GitHub
# (not GITHUB_CLIENT_ID in this app — those vars are unused by website/src)