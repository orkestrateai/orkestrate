# Orkestrate Memory Server Configuration

# Server port (default: 3001)
PORT=3001

# OpenCode provider requires the opencode CLI to be installed
# and configured with your credentials.
# No API key needed here — OpenCode manages its own auth.
# Install: npm install -g opencode
# Configure: opencode config
